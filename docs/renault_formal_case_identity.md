# Renault formal case identity

`FORMAL_CASE_IDENTITY_RESOLVED`

The formal processed Renault cases are `210202` and `210628`. Their formal-instance
hashes, exact identity mappings, baseline artifacts, and compatibility provenance use
those case identifiers.

Case `210712` is available in the raw source archive but is not part of the current
formal processing chain. It is not rebuilt or used by this audit.

The source archive is represented portably by the filename `instances.tar.gz`, the
logical reference `external://renault-raw/instances.tar.gz`, and its immutable SHA-256.
The path normalization changes provenance serialization only; the scientific payload
is unchanged.
